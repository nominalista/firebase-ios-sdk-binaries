import XCTest
@testable import firebase_ios_sdk_binaries

final class firebase_ios_sdk_binariesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(firebase_ios_sdk_binaries().text, "Hello, World!")
    }
}
